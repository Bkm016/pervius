[package]
name = "egui-keybind"
version = "0.2.0"
edition = "2024"
description = "Declarative keybinding definitions for egui"
license = "MIT"

[dependencies]
egui = "0.34"
serde = { version = "1", optional = true }

[features]
serde = ["dep:serde"]
